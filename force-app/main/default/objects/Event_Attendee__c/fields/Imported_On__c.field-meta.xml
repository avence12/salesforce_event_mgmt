<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Imported_On__c</fullName>
    <label>Imported On</label>
    <description>When this attendee was last seen in an uploaded file. Set by Apex rather than defaulted, so a re-import refreshes it — paired with Source_File__c it says which upload the current values came from.</description>
    <type>DateTime</type>
    <required>false</required>
    <trackHistory>false</trackHistory>
</CustomField>
