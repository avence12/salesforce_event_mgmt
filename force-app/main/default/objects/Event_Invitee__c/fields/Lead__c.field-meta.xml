<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lead__c</fullName>
    <label>Lead</label>
    <description>Set instead of Contact for an invitee with no customer relationship — a professor, researcher, speaker or press contact. Account is a transacting customer by definition, so these people cannot be Contacts. Exactly one of Contact__c / Lead__c is populated; the Invitee_Is_Contact_Xor_Lead validation rule enforces it.</description>
    <type>Lookup</type>
    <referenceTo>Lead</referenceTo>
    <relationshipLabel>Event Invitees</relationshipLabel>
    <relationshipName>Event_Invitees</relationshipName>
    <required>false</required>
    <deleteConstraint>Restrict</deleteConstraint>
    <trackHistory>false</trackHistory>
</CustomField>
